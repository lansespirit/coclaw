---
title: "Windows: 'openclaw' is not recognized after install"
description: "Fix Windows PATH issues after installing OpenClaw globally (npm prefix not on PATH, terminal not restarted, or multiple Node installs)."
seoTitle: "Fix Windows: 'openclaw' is not recognized after install"
seoDescription: "Fix Windows PATH issues after installing OpenClaw globally (npm prefix not on PATH, terminal not restarted, or multiple Node installs)."
reviewedBy:
  - "CoClaw Editorial Team"
reviewedAt: 2026-03-14
testedOn:
  - "Windows"
verificationStatus: "reviewed"
contentOwner: "CoClaw Editorial Team"
reviewCadenceDays: 45

kind: "solution"
component: "install"
severity: "high"
os:
  - "windows"
channel: []
errorSignatures:
  - "is not recognized as an internal or external command"
keywords:
  - "windows"
  - "PATH"
  - "npm"
publishDate: 2026-02-03
lastUpdated: 2026-02-03
author: "CoClaw Team"
related:
  guides:
    - "/guides/openclaw-installation-troubleshooting"
    - "/guides/openclaw-windows-native-vs-wsl2"
    - "/guides/openclaw-native-windows-field-guide"
  docs:
    - "https://docs.openclaw.ai/install/installer"
layout: ../../../layouts/TroubleshootingLayout.astro
---

## Symptoms

- PowerShell shows:
  - `'openclaw' is not recognized as an internal or external command`
- You just installed OpenClaw, but the command isn't available.

## Cause

On Windows, global npm installs place executables into a global bin folder (commonly `%AppData%\\npm`).
If that folder is not on PATH (or you haven't restarted the terminal), `openclaw` won't be found.

## Fix

### 1) Restart PowerShell/Windows Terminal

Close all terminals and reopen one (PATH changes don't always apply to existing shells).

### 2) Find your npm global prefix

```powershell
npm config get prefix
```

Your global bin folder is usually `<prefix>\\bin` (or `%AppData%\\npm` depending on Node setup).

### 3) Add the global bin folder to PATH

Add it via Windows Environment Variables UI, then reopen the terminal.

## Verify

```powershell
where openclaw
openclaw --help
```



## Related

- [OpenClaw Installation Troubleshooting: Node/NPM, PATH, Windows (WSL2), and Docker](/guides/openclaw-installation-troubleshooting)
- [OpenClaw on Windows: Native vs WSL2, Install Paths, and When to Switch](/guides/openclaw-windows-native-vs-wsl2)
- [OpenClaw on Native Windows: PATH, Scheduled Tasks, Node Host, and the Real Failure Modes](/guides/openclaw-native-windows-field-guide)
