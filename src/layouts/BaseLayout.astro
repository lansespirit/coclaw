---
import { Navbar } from '../components/Navbar';
import { Footer } from '../components/Footer';
import GoogleAnalytics from '../components/analytics/GoogleAnalytics.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  keywords?: string;
  lang?: string;
  robots?: string;
  ogLocale?: string;
  type?: 'website' | 'article';
}

const {
  title,
  description,
  ogImage,
  keywords,
  lang = 'en',
  robots = 'index, follow',
  ogLocale = 'en_US',
  type = 'website',
} = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const defaultOgImage = '/images/og/og-main.png'; // Default OG image
const finalOgImage = ogImage || defaultOgImage;

// Cloudflare Pages injects these at build time. Keeping them in the HTML makes it easy
// to verify which deployment is currently serving traffic when debugging env issues.
const cfCommit = process.env.CF_PAGES_COMMIT_SHA;
const cfBranch = process.env.CF_PAGES_BRANCH;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <!-- Theme script -->
    <script is:inline>
      (() => {
        try {
          const stored = localStorage.getItem('theme');
          // Default to dark unless user explicitly chose a theme.
          const shouldDark = stored ? stored === 'dark' : true;
          document.documentElement.classList.toggle('dark', shouldDark);
        } catch {}
      })();
    </script>

    <title>{title}</title>
    <meta name="title" content={title} />
    {description && <meta name="description" content={description} />}
    {keywords && <meta name="keywords" content={keywords} />}
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content={type} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:site_name" content="CoClaw" />
    {description && <meta property="og:description" content={description} />}
    <meta property="og:image" content={new URL(finalOgImage, Astro.site)} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content={ogLocale} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    {description && <meta property="twitter:description" content={description} />}
    <meta property="twitter:image" content={new URL(finalOgImage, Astro.site)} />

    <!-- Additional SEO -->
    <meta name="robots" content={robots} />
    <meta name="author" content="CoClaw Community" />
    <meta name="theme-color" content="#006fee" />
    {cfCommit && <meta name="cf-pages-commit-sha" content={cfCommit} />}
    {cfBranch && <meta name="cf-pages-branch" content={cfBranch} />}

    <!-- Per-page additions (JSON-LD, extra meta tags, etc.) -->
    <GoogleAnalytics />
    <slot name="head" />
  </head>
  <body class="bg-background text-foreground min-h-screen flex flex-col">
    <slot name="topbar" />
    <header class="sticky top-0 z-50">
      <Navbar client:load />
    </header>

    <main class="flex-grow">
      <slot />
    </main>
    <Footer client:visible />
  </body>
</html>
